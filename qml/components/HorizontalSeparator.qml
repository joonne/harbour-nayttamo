/* A horizontal separator to add padding between elements in column,
 expects that the parent is a column or the page. */

import QtQuick 2.0
import Sailfish.Silica 1.0

Item {
    height: Theme.paddingMedium
    width: parent.width
}

